#include "Triangulation.h"
// Class for converting triangulation data from OBJ to STL format
class ObjtoStl{
    public:
    // Method to convert triangulation data from OBJ to STL format
       void writer(Triangulation &triangulation);
};